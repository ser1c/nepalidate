## Submission Comments

This is a new submission.

The `nepalidate` package provides fast and dependency-free functions for converting between Nepali (BS) and Gregorian (AD) dates.

I have run `devtools::check()` locally with no errors, warnings, or notes.

I have also checked the package using `rhub::rhub_check()` . The package passed on linux , m1-san,macos,macos-arm64, windows and atlas.

Thank you for your consideration.